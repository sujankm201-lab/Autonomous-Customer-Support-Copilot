# Frontend

Frontend is implemented with React + Vite + TypeScript + Tailwind CSS in later phases. No UI code is implemented in Phase 1.

Planned structure:

- `frontend/src/` — React source files
- `frontend/public/` — static assets
- `frontend/tests/` — frontend tests (Jest / Playwright)
