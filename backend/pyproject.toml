[tool.poetry]
name = "autonomous-copilot-backend"
version = "0.1.0"
description = "Backend for Autonomous Customer Support Copilot"
authors = ["Intern <intern@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
